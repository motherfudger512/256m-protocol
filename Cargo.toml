[workspace]
members = ["programs/*"]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1

# blake3 pin removed — [patch.crates-io] can't point back to crates.io.
# If blake3 edition2024 causes issues, pin it via a workspace dependency instead.

